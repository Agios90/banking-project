To run this project, navigate to the Project1 directory in the command prompt, then log into MySQL and run the database file with

source afdemp_java_1.sql

Then exit MySQL and run the project with

java -jar dist/Project1.jar

The output files should be generated inside the project's main directory.